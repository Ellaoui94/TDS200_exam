import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TDS200_h22_ditt-1152',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
